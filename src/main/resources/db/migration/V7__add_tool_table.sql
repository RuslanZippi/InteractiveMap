CREATE TABLE tool (
                       id INT NOT NULL,
                       name VARCHAR(255) NULL,
                       count INT NULL,
                       position VARCHAR(255)NULL,
                       PRIMARY KEY (id)
);
