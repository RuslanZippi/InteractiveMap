CREATE TABLE shelf (
                         id INT NOT NULL,
                         name VARCHAR(255) NULL,
                         PRIMARY KEY (id)
);

CREATE TABLE stand (
    id INT NOT NULL,
    name VARCHAR(255) NULL,
    PRIMARY KEY(id)
);